import React from "react";

const Spinner = () => {
  return (
    <div className="loadingSpinnerContainer">
      <div className="loadingSpinner">SPINNY SPIN</div>
    </div>
  );
};

export default Spinner;
